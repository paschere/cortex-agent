node_modules
.next
.turbo
dist
**/*.tsbuildinfo
coverage
playwright-report
.env
.env.local
.env.*.local
.env.*
*.log
.DS_Store
.vercel
.wrangler
.env.vault
.supabase
infra/supabase/.branches
infra/supabase/.temp
.tmp-vexa-gmeet
.pnpm-store

# Claude Code workspace (workflow worktrees, session state)
.claude/

.vercel
.env*

# Salida de compilación de un experimento de pruebas e2e con su propio directorio.
# El mecanismo (NEXT_DIST_DIR) ya no está en next.config.mjs; la línea se queda
# para que lo que quedó en disco no se cuele en un commit por descuido.
.next-e2e
