'use client';
import { Button } from '@/components/ui/button';

export function DriveConnect() {
  return (
    <Button variant="outline" disabled title="Google Drive integration coming soon">
      Connect Drive folder (coming soon)
    </Button>
  );
}
